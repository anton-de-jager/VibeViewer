import { Route } from '@angular/router';
import { RefreshComponent } from 'app/modules/landing/refresh/refresh.component';

export const refreshRoutes: Route[] = [
    {
        path     : '',
        component: RefreshComponent
    }
];
